export default {
    _AddressPort: 3000,    /* Sets the default server port. */
    _ExposeNetwork: false, /* Toggle the visibility of your Network IP. */
    _QuietMode: false      /* Hide all Vinta.js console logs. */
}